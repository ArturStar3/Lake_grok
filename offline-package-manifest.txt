InfoLake offline package manifest (DEV mode)
Generated: 2026-07-27 16:48:48
Git branch: develop_report

Mode: DEV (docker-compose.yml, bind-mount frontend/backend/tileserver from disk)

Docker images in infolake_full_offline.tar:
  - maptiler/tileserver-gl:latest
  - infolake-backend:latest
  - infolake-frontend:latest

Archive:
  Stable:    .\infolake_full_offline.tar
  Timestamp: .\infolake_full_offline_20260727_164848.tar
  Size:      2757.6 MB

Copy to offline server (ALL required):
  1. infolake_full_offline.tar
  2. docker-compose.yml
  3. backend/ (with .env)
  4. frontend/ (source code — layers in tiles.js)
  5. tileserver/ (styles/, config.json — REQUIRED for map layers)
  6. tileserver/data/map.mbtiles (separate, large file)

On offline server (DEV):
  docker load -i infolake_full_offline.tar
  docker compose up -d --no-build --pull never

NEVER on offline machine:
  docker compose up --build
  docker compose build
  docker compose pull
  docker pull

Verify layers:
  dir tileserver\styles\overlay-roads.json
  curl.exe -s http://localhost:8080/styles/infolake-unified/style.json | findstr roads
